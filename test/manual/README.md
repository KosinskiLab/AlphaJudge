# Manual Benchmark Helpers

These scripts are developer-only helpers for reproducing benchmark comparisons
or regenerating frozen calibration constants. They are not run by pytest and are
not installed as AlphaJudge command-line tools.

Run them manually with explicit local input/output paths.
